
# Script generated by Blazemeter Chrome Extension

modules:
  nose:
    ignore-unknown-actions: true
  modules:
  cloud:
    token: 'f3e073d168f35ed0a0bd6eaf:1240407d4e3ffb1652304ef35c5ad421791ee94d5bd1fddae3f65776a1dfef753901f0ea'  # API id and API secret divided by :
    timeout: 10s  # BlazeMeter API client timeout
    browser-open: start  # auto-open browser on test start/end/both/none
    check-interval: 5s  # interval which Taurus uses to query test status from BlazeMeter
    public-report: false  # make test report public, disabled by default
    send-report-email: false  # send report email once test is finished, disabled by default
    request-logging-limit: 10240 # use this to dump more of request/response data into logs, for debugging

execution:
- executor: selenium
  scenario: DemoTest-Selenium
  blazegrid: true
  iterations: 1
  capabilities: 
    browserName: chrome

scenarios:
  DemoTest-Selenium:
    generate-flow-markers: true 
    headless: false
    timeout: 60s
    think-time: 0s
    requests:
    - label: Test
      actions:
      - go(chrome://newtab/)
      - go(https://www.google.com/)
      - clickByName(q)
      - typeByName(q): "Selenium Test"
      - keysByName(q): KEY_ENTER